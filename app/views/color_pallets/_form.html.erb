<%= form_for(@color_pallet) do |f| %>
  <% if @color_pallet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@color_pallet.errors.count, "error") %> prohibited this color_pallet from being saved:</h2>

      <ul>
      <% @color_pallet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color %><br />
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
